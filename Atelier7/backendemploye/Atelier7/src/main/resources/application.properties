spring.application.name=Atelier7

# ESSALHI SALMA
# ====================== SERVER ======================
server.port=8080

# ====================== DATABASE ======================
# DATABASE
spring.datasource.url=jdbc:mysql://localhost:3306/employees_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Africa/Casablanca
spring.datasource.username=root
spring.datasource.password=


# ====================== JPA / HIBERNATE ======================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
# Hibernate détecte automatiquement le dialect MySQL

# Pour voir les param?tres bind?s dans les logs (tr?s utile en dev)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

